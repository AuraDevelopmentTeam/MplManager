apply from: "../GradleCommon/common.gradle"

repositories {
    // Additional repositories
}

dependencies {
    shadowRelocate "eu.mikroskeem:picomaven:${picomaven_version}"
    
    compileOnly "de.adrodoc55.mpl:mpl-compiler:${MPL_version}"
}
